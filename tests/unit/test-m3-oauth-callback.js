import assert from 'node:assert';
import { GET } from '../../src/app/[locale]/auth/callback/route.js';

// Mock Supabase client module
// Note: We need to mock createClient from '@/lib/supabase/server'
